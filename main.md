You implemented naming, but the result is still not acceptable as final.

Problem:
You returned final names, but you did NOT clearly prove the derivation logic from the attached OFW standards and CZ/Synapse naming standards.
I do not want customer_orders-specific hardcoding.
I want rule-based derivation that is transparent, reusable, and auditable.

Your task now is to REWORK the implementation so the logic is rule-driven, not scenario-specific.

Requirements

1. Read the attached standards again and extract the exact naming rules into code-level logic.
   Do not just output final names.

2. Before changing code, produce a “Naming Rule Extraction” section with:
   - OFW job type patterns found
   - meaning of each job type
   - JSON onboarding naming rules
   - pipeline onboarding naming rules
   - ADLS naming rules
   - Synapse base/stage/ctas/view naming rules
   - lower/upper case requirements
   - optional fields such as tenant id

3. Then produce a “Derivation for customer_orders” section with:
   - candidate job types considered
   - selected job type
   - why selected
   - framework token selected
   - malcode selected
   - tenant included or excluded and why
   - description token
   - function token
   - final OFW job name
   - final ADLS object name
   - final Synapse base table
   - final Synapse stage table
   - final Synapse ctas table
   - final DAC/simple view

4. Then change the code so the implementation is generalized:
   - no customer_orders-specific hardcoding
   - no direct string comparison only for this scenario
   - naming should be generated by reusable helper functions
   - helper functions should accept structured inputs and return derived names

5. Create or update reusable helpers for:
   - deriveOfwJobName(...)
   - deriveOnboardingFileName(...)
   - deriveAdlsObjectName(...)
   - deriveSynapseNames(...)
   - normalizeDescriptionToken(...)
   - normalizeFunctionToken(...)

6. Add tests that prove the logic is generalized, not hardcoded.
   Add at least these test groups:
   - customer_orders
   - one different bronze→silver scenario
   - one scenario where tenant is applicable
   - one scenario where tenant is not applicable
   - one scenario verifying lowercase ADLS/Synapse object names
   - one scenario verifying base_table / stage / ctas / DAC naming
   - one negative test showing invalid inputs are rejected cleanly

7. Show exact old vs new behavior:
   - before: customer_orders-specific result
   - after: generalized rule-driven derivation

8. Important:
   - Do not ask me for malcode/tenant rules unless the screenshots truly leave ambiguity.
   - If ambiguous, list the ambiguity explicitly and stop there.
   - Do not silently invent rules.

Return format

A. Naming Rule Extraction
B. Derivation for customer_orders
C. Code changes made
D. Tests added
E. Proof this is generalized and not hardcoded
F. Remaining ambiguities, if any
